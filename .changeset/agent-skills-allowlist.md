---
"@moonshot-ai/kimi-code": patch
---

Agents can now declare a skills allowlist in their agent file (or via TeamHire when hiring): only the listed skills are available to that agent, so team members get scoped to the skills you intend instead of the full catalog. Add `skills` to the agent profile to restrict it.
