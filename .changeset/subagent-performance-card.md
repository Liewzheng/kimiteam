---
"@moonshot-ai/kimi-code": patch
---

At dispatch, a team member's prompt now includes its own performance card — average score over its scoring window plus team rank — so each subagent knows how it is tracking without ever seeing another member's scores. The card appears once the profile has any scores; rank needs at least three.
