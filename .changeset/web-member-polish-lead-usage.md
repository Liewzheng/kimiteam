---
"@moonshot-ai/kimi-code": minor
---

web: Show the lead's token usage in /usage, display each member's actually-used model (falling back to preference), let members be edited with a model dropdown and a prompt "polish" modal (confirm/cancel), and keep the workflow output structured with a bounded scrollable area.
