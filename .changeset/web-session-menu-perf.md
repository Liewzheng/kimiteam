---
"@moonshot-ai/kimi-code": minor
---

web: Add multi-select and a right-click menu to the session list (batch archive / copy IDs), and fix scroll and typing jank in long conversations by restoring paint containment, skipping TOC layout work when hidden, coalescing streamed parsing, and debouncing draft saves.
