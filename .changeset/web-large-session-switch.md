---
"@moonshot-ai/kimi-web": patch
---

Fix the web chat showing most of a large session's history as blank until scrolled after switching to it: scroll restore now pins to the same messages, and the pre-render window scales with message height.
