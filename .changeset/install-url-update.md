---
"@moonshot-ai/kimi-code": patch
---

Point the kimiteam install scripts and install documentation at the kimiteam repository main branch, so the rolling dev release and the raw install commands resolve from the new repo home.
