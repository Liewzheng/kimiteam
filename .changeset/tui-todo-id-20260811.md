---
"@moonshot-ai/kimi-code": patch
---

Show each todo's stable id in the in-session Todo panel, dimmed next to the title, so users can reference specific tasks back to the model. Todos from older sessions without an id render unchanged.
