---
"@moonshot-ai/kimi-code": patch
---

Allow the /team panel to open while the agent is streaming: the view refreshes automatically and Ctrl+C inside the panel cancels the running turn. Run /team mid-turn to watch and interrupt from the panel.
