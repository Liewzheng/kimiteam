---
"@moonshot-ai/kimi-code": patch
---

Added a Windows PowerShell installer and launcher (`scripts/install-kimiteam.ps1` + `~/.kimi-code/bin/kimiteam.ps1`) mirroring the bash installer, so Windows users can install kimiteam natively without WSL or Git Bash.
