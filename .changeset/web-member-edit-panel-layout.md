---
"@moonshot-ai/kimi-code": patch
---

Fix the team member edit panel in the web UI rendering with overlapping fields (thinking mode / token usage / workflow sections colliding) when opened from the member detail view.
