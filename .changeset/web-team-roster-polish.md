---
"@moonshot-ai/kimi-code": minor
---

web: Trim the team roster to three states with grey-tinted card colours, show member display names, keep workflow output in a tool-card-like bordered box, and render TeamMessage cards with markdown instead of raw JSON. TodoList is now main-agent only, providers get a UI to persist to config.toml, and the lead's usage shows in /usage.
