---
"@moonshot-ai/kimi-web": minor
---

Add a per-session wide/narrow layout toggle to the web conversation header, so wide markdown tables can render at their natural width. Use the expand/collapse icon next to the conversation title to switch; each session remembers its choice, and the default narrow layout keeps wide tables scrolling inside the reading column.
