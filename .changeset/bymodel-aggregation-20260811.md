---
"@moonshot-ai/kimi-code": patch
---

Persist each member's per-model TeamScore breakdown and keep it in sync as scores and work shifts are recorded, so the breakdown is no longer empty for profiles whose entries already carry a model attribution.
