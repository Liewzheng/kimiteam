---
"@moonshot-ai/kimi-code": patch
---

Fix the web settings Providers tab wrongly showing "No providers configured yet" until the provider manager was opened once; configured providers now load with the app.
