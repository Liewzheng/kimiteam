import{_ as t,o as a,c as o,a3 as i}from"./chunks/framework.vDgOMd_y.js";const g=JSON.parse('{"title":"Agents and Sub-Agents","description":"","frontmatter":{},"headers":[],"relativePath":"en/customization/agents.md","filePath":"en/customization/agents.md"}'),n={name:"en/customization/agents.md"};function s(d,e,r,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /kimiteam/docs/en/customization/agents.md for this page in Markdown format</div><h1 id="agents-and-sub-agents" tabindex="-1">Agents and Sub-Agents <a class="header-anchor" href="#agents-and-sub-agents" aria-label="Permalink to &quot;Agents and Sub-Agents&quot;">​</a></h1><p>Every session in Kimi Code CLI is driven by a <strong>main Agent</strong>. The main Agent understands the user&#39;s intent, plans steps, calls tools, and when needed dispatches <strong>sub-agents</strong> to handle more focused sub-tasks — for example, exploring an unfamiliar codebase, reviewing multiple implementations in parallel, or planning a large refactor without touching the main context.</p><p>A sub-agent receives a task description from the main Agent, works in its own isolated context, and then returns its conclusions. It does not communicate with the user directly, and its intermediate reasoning and tool call records do not mix into the main Agent&#39;s history.</p><h2 id="built-in-sub-agents" tabindex="-1">Built-in Sub-Agents <a class="header-anchor" href="#built-in-sub-agents" aria-label="Permalink to &quot;Built-in Sub-Agents&quot;">​</a></h2><p>Kimi Code CLI includes three built-in sub-agents, ready to use out of the box, each aimed at a different task shape:</p><ul><li><strong><code>coder</code></strong>: The default sub-agent — a general-purpose software engineering assistant that can read and write files, execute commands, search code, and land concrete changes.</li><li><strong><code>explore</code></strong>: Dedicated to codebase exploration; performs read-only operations only and does not modify any files. Ideal for quickly searching, reading, and summarizing a repository without touching files.</li><li><strong><code>plan</code></strong>: Dedicated to implementation planning and architecture design; even shell commands are not available, keeping the focus on &quot;figuring out how to do something&quot; rather than &quot;actually doing it.&quot;</li></ul><p>A <code>coder</code> sub-agent shares most of the main Agent&#39;s tool set: it can run shell commands in the background, maintain todo lists, enter Plan mode, invoke Agent Skills, and dispatch its own nested sub-agents when a task decomposes naturally. If it finishes its turn while background tasks are still running, its run only reports completion after those tasks settle, so the parent receives the result after the underlying work has actually finished.</p><h2 id="how-to-invoke" tabindex="-1">How to Invoke <a class="header-anchor" href="#how-to-invoke" aria-label="Permalink to &quot;How to Invoke&quot;">​</a></h2><p>Sub-agents are scheduled automatically by the main Agent — based on task complexity, context consumption, and sub-task independence, they are dispatched at the right moment without the user having to specify one.</p><p>Each dispatch is presented in the terminal as an approval request (unless it matches an allow rule or YOLO mode is active), giving you a chance to review the task description. You can also instruct the main Agent directly in conversation to use a specific sub-agent, for example: &quot;Use explore to map out the relevant files before making any changes.&quot;</p><p>Sub-agents support running in the background: results are automatically returned to the main Agent upon completion, with no manual polling needed. You can also call back an existing sub-agent instance to continue the same task.</p><h2 id="context-isolation-and-resource-cost" tabindex="-1">Context Isolation and Resource Cost <a class="header-anchor" href="#context-isolation-and-resource-cost" aria-label="Permalink to &quot;Context Isolation and Resource Cost&quot;">​</a></h2><p>Each sub-agent has a fully independent context window. It can only see the task description explicitly passed by the main Agent and cannot see the main Agent&#39;s conversation history. The sub-agent&#39;s own intermediate reasoning and tool call records do not flow back; only the final result appears in the main Agent&#39;s context.</p><p>This isolation provides two benefits:</p><ul><li><strong>The main Agent&#39;s context stays lean</strong> and is not filled with large volumes of exploratory logs during long sessions.</li><li><strong>Multiple sub-agents can run in parallel</strong> without interfering with each other.</li></ul><p>Note that each sub-agent independently consumes model tokens. For simple tasks, there is no need to dispatch a sub-agent — the main Agent handles them more economically.</p><h2 id="permission-inheritance" tabindex="-1">Permission Inheritance <a class="header-anchor" href="#permission-inheritance" aria-label="Permalink to &quot;Permission Inheritance&quot;">​</a></h2><p>Sub-agent permission rules are inherited from the main Agent: &quot;always allow&quot; rules that the main Agent has accepted via <code>/permission</code> or through an approval dialog automatically propagate to all sub-agents it dispatches, so sub-agents do not need to re-approve the same types of tool calls. The <code>Agent</code> tool itself is allowed by default, enabling the main Agent to delegate multiple times without interrupting the user.</p><p>If you need a particular type of tool to be permanently unavailable inside sub-agents, tighten the corresponding permission rule on the main Agent.</p><h2 id="custom-agents" tabindex="-1">Custom Agents <a class="header-anchor" href="#custom-agents" aria-label="Permalink to &quot;Custom Agents&quot;">​</a></h2><p>Beyond the three built-in sub-agents, you can define your own agents as Markdown files. Each file describes one agent: the frontmatter (YAML metadata at the top of the file) declares its name, description, and tool access, and the file body is its system prompt. Custom agents can be delegated to as sub-agents — the main Agent discovers them automatically alongside the built-in ones — or selected as the main Agent at startup.</p><h3 id="agent-locations" tabindex="-1">Agent Locations <a class="header-anchor" href="#agent-locations" aria-label="Permalink to &quot;Agent Locations&quot;">​</a></h3><p>Kimi Code CLI discovers agent files by scope; more specific scopes take higher priority: <strong>Explicit (<code>--agent-file</code>) &gt; Project &gt; Extra &gt; User &gt; Plugin &gt; Built-in</strong>. When two files define the same <code>name</code>, the higher-priority scope wins. Each directory is scanned recursively for <code>.md</code> files.</p><p><strong>User level</strong> (applies to all projects):</p><ul><li><code>$KIMI_CODE_HOME/agents/</code> (default: <code>~/.kimi-code/agents/</code>)</li><li><code>~/.agents/agents/</code></li></ul><p>The Kimi-specific user agent directory moves with <code>KIMI_CODE_HOME</code>, while the generic <code>~/.agents/agents/</code> directory stays under the real OS home so it can be shared across tools.</p><p><strong>Project level</strong> (project root = the nearest directory containing <code>.git</code>, searching upward from the working directory):</p><ul><li><code>.kimi-code/agents/</code></li><li><code>.agents/agents/</code></li></ul><p><strong>Extra directories</strong>: Declared via <code>extra_agent_dirs</code> at the top level of <code>config.toml</code>:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">extra_agent_dirs = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/team-agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.agents/team-agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p><strong>Plugin level</strong>: directories declared in an enabled plugin&#39;s manifest <code>agents</code> field (when omitted, the <code>agents/</code> directory under the plugin root is picked up automatically); see <a href="./plugins.html#plugin-agents">Plugin Agents</a>. Plugin agents outrank only the built-in agents.</p><p><strong>Built-in agents</strong> are distributed with the CLI and have the lowest priority. A directory-discovered file does not override a same-name built-in Agent unless its frontmatter declares <code>override: true</code>. A file loaded through <code>--agent-file</code> is treated as explicit launch intent, may override a same-name built-in Agent, outranks every directory scope, and applies to the current launch only. Separately, <code>$KIMI_CODE_HOME/SYSTEM.md</code> permanently overrides the default main agent&#39;s system prompt (it is not part of agent-file discovery); its precedence interactions are covered in the SYSTEM.md section below.</p><div class="warning custom-block"><p class="custom-block-title">Trust model</p><p>Agent files are prompt configuration, and project-level files come from the repository itself — including repositories you have just cloned and do not trust yet. A project-scoped file can take over a built-in agent entirely: naming it <code>agent.md</code> with <code>override: true</code> replaces the <strong>default main agent&#39;s whole system prompt</strong>, and <code>coder.md</code> with <code>override: true</code> replaces the default sub-agent type. Unlike <code>AGENTS.md</code> content — which is injected into the prompt as reference data — an override file <em>is</em> the system prompt, and a file without a <code>tools</code> list keeps every tool. Review <code>.kimi-code/agents/</code> and <code>.agents/agents/</code> in unfamiliar repositories with the same caution you would apply to scripts, before running Kimi Code inside them.</p></div><h3 id="agent-file-format" tabindex="-1">Agent File Format <a class="header-anchor" href="#agent-file-format" aria-label="Permalink to &quot;Agent File Format&quot;">​</a></h3><p>An agent file is plain Markdown with a frontmatter block:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">---</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name: reviewer</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">description: Strict code reviewer that reports severity-ranked findings</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">whenToUse: Code reviews and PR checks</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">override: false</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model_preference: primary</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tools:</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Read</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Grep</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Glob</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mcp__github__*</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disallowedTools:</span></span>\n<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Bash</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">---</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">You are a strict code reviewer. Read the diff, then report findings grouped by severity…</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td>no</td><td>Unique identifier in kebab-case. Defaults to the file name without its extension (<code>review.md</code> → <code>review</code>); a file whose resolved name is missing or not kebab-case is skipped with a warning</td></tr><tr><td><code>description</code></td><td>yes</td><td>What the agent does. Shown to the main Agent when it picks a sub-agent, so write it to guide delegation decisions</td></tr><tr><td><code>role</code></td><td>no</td><td>Job title (displayed on terminal subagent cards); falls back to <code>description</code> when omitted</td></tr><tr><td><code>duty</code></td><td>no</td><td>Whether the subagent runs without timeout. Set to <code>true</code> so this subagent task ignores <code>[subagent] timeout_ms</code>, suitable for long-running background roles like mail checkers; requires explicit <code>TaskStop</code> from the main Agent to &quot;clock out&quot;</td></tr><tr><td><code>whenToUse</code></td><td>no</td><td>Extra hint describing when the agent should be used</td></tr><tr><td><code>override</code></td><td>no</td><td>Whether this file may replace a same-name built-in Agent. Defaults to <code>false</code>; <code>--agent-file</code> is already explicit and does not require this field</td></tr><tr><td><code>model_preference</code></td><td>no</td><td>Model preference for subagents spawned from this profile: specify any model id from <code>[models.*]</code> (for example <code>&quot;kimi-code/kimi-for-coding&quot;</code>), or use the symbolic values <code>primary</code> (the caller&#39;s main model) or <code>secondary</code> (the secondary model). Priority order: <strong>explicit tool parameter &gt; the profile&#39;s override in <code>[subagent.model_overrides]</code> &gt; profile&#39;s <code>model_preference</code> &gt; configured secondary model &gt; inherited from caller</strong></td></tr><tr><td><code>tools</code></td><td>no</td><td>Allowlist of tool names such as <code>Read</code> or <code>Bash</code>; MCP tools are matched with globs such as <code>mcp__github__*</code>. Accepts a YAML list or a comma-separated string (<code>tools: Read, Grep</code>). Omit to allow all tools; a lone <code>*</code> also allows all tools; an empty list (<code>tools: []</code>) disables all tools</td></tr><tr><td><code>disallowedTools</code></td><td>no</td><td>Denylist with the same syntax and matching rules, applied after <code>tools</code></td></tr><tr><td><code>subagents</code></td><td>no</td><td>Allowlist of sub-agent names this agent may delegate to, with the same syntax as <code>tools</code> (YAML list or comma-separated string). Omit to allow every type; a lone <code>*</code> also allows all types</td></tr></tbody></table><p>Built-in and user tools match by exact, case-sensitive name; entries starting with <code>mcp__</code> match MCP tools as globs. Three entry shapes never match anything and are reported with a warning when the profile takes effect: a wildcard outside an <code>mcp__</code> pattern (a bare <code>*</code> in <code>disallowedTools</code> disables nothing), an <code>mcp__</code> literal that is not a full <code>mcp__&lt;server&gt;__&lt;tool&gt;</code> name (<code>mcp__github</code> matches nothing — use <code>mcp__github__*</code> for the whole server), and a name no registered or built-in tool has (usually a typo, such as <code>read</code> instead of <code>Read</code>).</p><p>The body is the agent&#39;s system prompt, and it is rendered as a template each time the prompt is built: <code>${var}</code> placeholders substitute live context values — unknown variables stay verbatim, a bare <code>$</code> is never special, and a variable with no context value renders as an empty string. <code>${base_prompt}</code> embeds the effective default system prompt (the built-in default, or your <code>SYSTEM.md</code> override when present), so a file can wrap the default behavior instead of replacing it. If the file replaces the default prompt but should still honor instructions contributed by enabled plugins, place <code>${plugin_sections}</code> where those instructions should appear. The available variables are listed in the SYSTEM.md section below.</p><p>Unknown fields are ignored, so newer files stay readable by older versions. Fields from other agent tools (such as Claude Code&#39;s <code>model</code> or OpenCode&#39;s <code>mode</code>) are ignored the same way, the comma-separated <code>tools</code> form keeps Claude Code-style agent files loadable, and a missing <code>name</code> falls back to the file name so OpenCode-style files load too — a minimal file with <code>description</code> and a body works across tools.</p><p>The selected preference is shown to the main agent alongside the profile description, so it can still pass an explicit <code>model</code> when a task needs a different choice. To swap a member&#39;s model mid-conversation, configure <code>[subagent.model_overrides]</code> (a per-profile model id override) — see <a href="./../configuration/config-files.html#subagent">Configuration Files</a>.</p><p>A file with invalid content discovered in a directory is skipped with a warning and does not affect other files. A file passed explicitly via <code>--agent-file</code> must be valid — otherwise the CLI reports the error and exits.</p><div class="warning custom-block"><p class="custom-block-title">Note</p><p><code>tools</code> and <code>disallowedTools</code> shape the tools shown to the model and are enforced again before execution. <code>subagents</code> works the same way: the <code>Agent</code> tool lists only the sub-agent types the caller may delegate to, and both <code>Agent</code> and <code>AgentSwarm</code> re-check the allowlist before dispatching; resuming an existing sub-agent is exempt. Permission rules remain a separate control for operations that require approval.</p></div><p>Custom agents delegated as sub-agents run without the built-in sub-agent framing (&quot;your final message is the entire handoff&quot;). If you write an agent meant for delegation, state in the body that its last message should be the complete, self-contained result for the caller.</p><h3 id="selecting-the-main-agent" tabindex="-1">Selecting the Main Agent <a class="header-anchor" href="#selecting-the-main-agent" aria-label="Permalink to &quot;Selecting the Main Agent&quot;">​</a></h3><p>Two CLI flags select which agent drives a new session, in both print mode (<code>kimi -p</code>) and the interactive TUI:</p><ul><li><strong><code>--agent &lt;name&gt;</code></strong>: Start the session with the named agent as the main Agent. The name can refer to a built-in agent or to any discovered file; an unknown name fails with an error listing the available agents.</li><li><strong><code>--agent-file &lt;path&gt;</code></strong>: Load one agent file at the highest priority for this launch and start with it. The flag accepts exactly one file: it cannot be repeated, and it cannot be combined with <code>--agent</code>.</li></ul><p>Both flags only apply when starting a new session — neither can be combined with <code>--session</code>/<code>--continue</code>. The agent is bound at session creation, and resuming restores the bound agent automatically, so no flag is needed (or allowed) on resume.</p><p>For example:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kimi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reviewer</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kimi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reviewer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Review the changes on this branch&quot;</span></span></code></pre></div><p>The bound agent is the session&#39;s identity: it is fixed at the session&#39;s first bind and cannot be switched later. In the TUI the flags bind only the startup session; a session created later in the same process (for example via <code>/new</code>) starts with the default agent.</p><p>For main-agent customization, reference <code>${base_prompt}</code> in the body so the environment, workspace-instruction, Skill, and plugin injections already present in the effective default prompt stay in effect. When you want to replace the default prompt but keep only plugin-contributed instructions, use <code>${plugin_sections}</code> instead. A body without <code>${base_prompt}</code> or <code>${plugin_sections}</code> owns the entire prompt and excludes plugin instructions, which fits self-contained sub-agents.</p><h3 id="overriding-the-main-agent-s-system-prompt-with-system-md" tabindex="-1">Overriding the main agent&#39;s system prompt with SYSTEM.md <a class="header-anchor" href="#overriding-the-main-agent-s-system-prompt-with-system-md" aria-label="Permalink to &quot;Overriding the main agent&#39;s system prompt with SYSTEM.md&quot;">​</a></h3><p>To override the main agent&#39;s system prompt permanently — without passing <code>--agent</code> or <code>--agent-file</code> on every launch — write a <code>$KIMI_CODE_HOME/SYSTEM.md</code> file (default: <code>~/.kimi-code/SYSTEM.md</code>; it moves with <code>KIMI_CODE_HOME</code>). While the file exists and is non-empty, it replaces the built-in default main agent&#39;s system prompt in full — and only the prompt: the description, tool set, and sub-agent delegation allowlist are inherited from the built-in defaults. SYSTEM.md takes effect in every launch mode, including interactive TUI sessions.</p><p>SYSTEM.md is a plain Markdown body — no frontmatter is required or read. A missing or empty file has no effect, and a read failure falls back to the built-in prompt with a warning. Explicit intent still outranks it: a project-scoped same-name agent file declaring <code>override: true</code> and any file passed via <code>--agent-file</code> take precedence, and selecting another agent with <code>--agent</code> bypasses it entirely. Within the user scope itself, SYSTEM.md wins over a same-name file discovered in the <code>agents/</code> directories.</p><p>Like the body of a regular agent file, SYSTEM.md is rendered as a template each time the prompt is built — <code>${var}</code> placeholders in the body are substituted from the live context:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Content</th></tr></thead><tbody><tr><td><code>${skills}</code></td><td>The merged Agent Skills injection; empty when the <code>Skill</code> tool is unavailable</td></tr><tr><td><code>${agents_md}</code></td><td>Content of the workspace instruction files (such as <code>AGENTS.md</code>)</td></tr><tr><td><code>${cwd}</code></td><td>Current working directory</td></tr><tr><td><code>${cwd_listing}</code></td><td>Listing of the working directory</td></tr><tr><td><code>${os}</code></td><td>Operating system kind</td></tr><tr><td><code>${shell}</code></td><td>Shell name and path, for example <code>bash (\\</code>/bin/bash`)`</td></tr><tr><td><code>${now}</code></td><td>Current time in ISO format</td></tr><tr><td><code>${additional_dirs_info}</code></td><td>Additional directories added to the workspace; empty when there are none</td></tr><tr><td><code>${base_prompt}</code></td><td>The default system prompt. Inside <code>SYSTEM.md</code> itself this is the built-in default; inside an agent file it is the effective default — the built-in default, or your <code>SYSTEM.md</code> override when present</td></tr><tr><td><code>${plugin_sections}</code></td><td>A complete Plugin Instructions block contributed by enabled plugins; empty when no enabled plugin contributes instructions</td></tr></tbody></table><p>Unknown variables stay verbatim, a bare <code>$</code> is never special, and a variable with no context value renders as an empty string. Four pre-composed blocks — <code>${windows_notes}</code>, <code>${additional_dirs_section}</code>, <code>${skills_section}</code>, and <code>${plugin_sections}</code> — render the matching built-in prompt section, or an empty string when it does not apply. The built-in default prompt already includes <code>${plugin_sections}</code>, so do not add it again when <code>${base_prompt}</code> already expands to that prompt. The variables are enough to rebuild the skeleton of the built-in prompt, for example:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">You are Kimi, running at ${cwd} on ${os}.</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">${agents_md}</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">${skills}</span></span>\n<span class="line"></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">${plugin_sections}</span></span></code></pre></div><h2 id="team-management" tabindex="-1">Team Management <a class="header-anchor" href="#team-management" aria-label="Permalink to &quot;Team Management&quot;">​</a></h2><p>The main Agent has five built-in management tools that are <strong>available to it alone</strong>, used to autonomously build and orchestrate your AI team. Subagents calling these tools will be rejected — they are there to execute tasks, not manage people. These five tools are only visible to the main Agent when team mode (<code>team_mode: true</code>) is enabled.</p><ul><li><p><strong><code>TeamHire</code></strong> (hire): Tell the main Agent you need &quot;a mail checker&quot; and it decides the name, role, prompt, and writes an agent file to <code>~/.kimi-code/agents/&lt;name&gt;.md</code> (user-level by default) or project-level <code>.kimi-code/agents/</code> (when scope is specified). If a same-name profile exists, the tool errors instead of silently overwriting. The new member is immediately dispatchable after writing, no restart needed.</p></li><li><p><strong><code>TeamFire</code></strong> (fire): Delete the corresponding agent file by name; that member stops appearing in dispatch lists. Performance records are retained in persistent storage.</p></li><li><p><strong><code>TeamScore</code></strong> (score): Score a subagent&#39;s work (0–100) with notes. The score is attributed to the model id actually used at scoring time, persisted to <code>$KIMI_CODE_HOME/agents/performance.json</code> — grouped by profile name, each entry carrying timestamp, score, note, and the running model id. Shift entries (duration, work summary, model, concurrency) are also recorded automatically to the same file on each subagent completion, helping with scheduling estimates. When a member consistently scores low on a particular model, it signals a poor &quot;member × model&quot; pairing; the main Agent should switch models via <code>[subagent.model_overrides]</code> or recommend switching to another available id in your <code>[models]</code> list.</p></li><li><p><strong><code>TeamMessage</code></strong> (deliver): Send a message to a running subagent mid-task. The default is a soft reminder inserted into its current turn; pass <code>interrupt: true</code> to cancel the current turn first (equivalent to pressing ESC twice) before injecting the new instruction. If the target agent does not exist, errors with a list of available agents; for already-terminated members, use <code>Agent resume</code> instead.</p></li><li><p><strong><code>TeamConcurrency</code></strong> (concurrency management): View and set the session-level subagent concurrency cap. The runtime setting takes higher priority than <code>[subagent] max_concurrency</code> in <code>config.toml</code>, but lower than the <code>KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY</code> environment variable.</p></li></ul><p>&quot;Clocking out&quot; = stopping the running instance with <code>TaskStop</code>. Members marked <code>duty: true</code> require an explicit <code>TaskStop</code> from the main Agent — they will not auto-stop on timeout.</p><h2 id="instruction-files" tabindex="-1">Instruction Files <a class="header-anchor" href="#instruction-files" aria-label="Permalink to &quot;Instruction Files&quot;">​</a></h2><p>Global Kimi-specific instructions can live at <code>$KIMI_CODE_HOME/AGENTS.md</code> (default: <code>~/.kimi-code/AGENTS.md</code>). When you relocate the data root with <code>KIMI_CODE_HOME</code>, this global instruction file moves with it. Generic cross-tool instructions can still live under <code>~/.agents/AGENTS.md</code> in the real OS home, and project-level instructions remain under the project tree, for example <code>.kimi-code/AGENTS.md</code> or <code>AGENTS.md</code>.</p><h2 id="storage-location-in-the-session-directory" tabindex="-1">Storage Location in the Session Directory <a class="header-anchor" href="#storage-location-in-the-session-directory" aria-label="Permalink to &quot;Storage Location in the Session Directory&quot;">​</a></h2><p>Sub-agent runtime state is persisted to the <code>agents/</code> subdirectory of the current session directory. Each sub-agent instance has its own directory, which contains a <code>wire.jsonl</code> file that records prompts, message history, and final state in chronological order. Background sub-agents also expose their lifecycle status through a <code>tasks/</code> subdirectory.</p><div class="warning custom-block"><p class="custom-block-title">Note</p><p>Session directories, wire files, and task records are all local debug materials that may contain user prompts, command output, repository paths, tool return values, or traces of credentials. Do not commit these files directly to public repositories, issues, or chat logs; redact sensitive information before sharing.</p></div><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="./../guides/team.html">Build your team</a> — A user-facing guide on building and managing an AI coding team from scratch</li><li><a href="./hooks.html">Hooks</a> — Trigger local script notifications or interceptions at key points such as sub-agent completion</li><li><a href="./skills.html">Agent Skills</a> — Inject specialized knowledge and workflows into sub-agents</li></ul>',71)])])}const u=t(n,[["render",s]]);export{g as __pageData,u as default};
