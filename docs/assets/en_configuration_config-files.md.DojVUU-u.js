import{_ as t,o as i,c as s,a3 as o}from"./chunks/framework.vDgOMd_y.js";const k=JSON.parse('{"title":"Configuration files","description":"","frontmatter":{},"headers":[],"relativePath":"en/configuration/config-files.md","filePath":"en/configuration/config-files.md"}'),a={name:"en/configuration/config-files.md"};function d(n,e,l,r,c,h){return i(),s("div",null,[...e[0]||(e[0]=[o(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /kimiteam/docs/en/configuration/config-files.md for this page in Markdown format</div><h1 id="configuration-files" tabindex="-1">Configuration files <a class="header-anchor" href="#configuration-files" aria-label="Permalink to &quot;Configuration files&quot;">​</a></h1><p>Kimi Code CLI writes all long-term preferences — which model to use, which API key to fill in, how many steps an Agent can run per turn — into TOML (a plain-text configuration format with a clear structure) files. Change them once and they take effect on every startup. Agent and runtime settings live in <code>config.toml</code>; terminal-UI and client preferences (theme, editor, notifications, auto-update) live in a companion <code>tui.toml</code>.</p><p>Default location: <code>~/.kimi-code/config.toml</code>, created automatically on first run.</p><h2 id="config-file-location" tabindex="-1">Config file location <a class="header-anchor" href="#config-file-location" aria-label="Permalink to &quot;Config file location&quot;">​</a></h2><p>The CLI reads configuration from <code>~/.kimi-code/config.toml</code>. To relocate the data directory, override it with the <code>KIMI_CODE_HOME</code> environment variable:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> KIMI_CODE_HOME</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/path/to/kimi-home</span></span></code></pre></div><p>The config file path then becomes <code>$KIMI_CODE_HOME/config.toml</code>. Regardless of where the directory lives, the file name is always <code>config.toml</code>.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>TOML field names always use snake_case, for example <code>default_model</code> and <code>max_context_size</code>. If a key contains <code>.</code>, you must quote it — for example <code>[models.&quot;gpt-4.1&quot;]</code> — otherwise TOML treats <code>.</code> as a nested table separator.</p></div><h2 id="complete-example" tabindex="-1">Complete example <a class="header-anchor" href="#complete-example" aria-label="Permalink to &quot;Complete example&quot;">​</a></h2><p>The following example covers the most commonly used configuration fields. You can copy it and adjust as needed:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-code/k3&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_permission_mode = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;manual&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_plan_mode = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">merge_all_available_skills = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">telemetry = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;managed:kimi-code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.kimi.com/coding/v1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;kimi-code/k3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">provider = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;managed:kimi-code&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;k3&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1048576</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;always_thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;image_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;video_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_use&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">display_name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;K3&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">support_efforts = [ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_effort = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;kimi-code/kimi-for-coding&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">provider = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;managed:kimi-code&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-for-coding&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">262144</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;always_thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;image_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;video_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_use&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;kimi-code/kimi-for-coding-highspeed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">provider = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;managed:kimi-code&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-for-coding-highspeed&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">262144</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">capabilities = [ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;always_thinking&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;image_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;video_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_use&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">thinking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">enabled = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">effort = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;high&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">keep = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;all&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">loop_control</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_attempts_per_step = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">reserved_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50000</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">background</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_running_tasks = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">keep_alive_on_exit = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">moonshot_search</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.kimi.com/coding/v1/search&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">moonshot_fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.kimi.com/coding/v1/fetch&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allow&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Read&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash(rm -rf*)&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PreToolUse&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">matcher = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">command = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;node ~/.kimi-code/hooks/check-bash.mjs&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">timeout = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span></span></code></pre></div><h2 id="top-level-fields" tabindex="-1">Top-level fields <a class="header-anchor" href="#top-level-fields" aria-label="Permalink to &quot;Top-level fields&quot;">​</a></h2><p>Fields in the config file fall into two categories: <strong>top-level scalars</strong> that directly control default behavior, and <strong>nested tables</strong> (<code>providers</code>, <code>models</code>, <code>thinking</code>, etc.) that each have their own structure, described individually in the sections below.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>default_model</code></td><td><code>string</code></td><td>—</td><td>Default model alias; must be defined in <code>models</code></td></tr><tr><td><code>default_permission_mode</code></td><td><code>string</code></td><td><code>manual</code></td><td>Default permission mode for new sessions; one of <code>manual</code> (prompt each time), <code>yolo</code> (auto-approve tool actions, but the agent may still ask questions), or <code>auto</code> (fully autonomous — the agent decides everything without asking)</td></tr><tr><td><code>default_plan_mode</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether new sessions start in Plan mode (produce a plan before executing) by default</td></tr><tr><td><code>merge_all_available_skills</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether to merge Agent Skills from all available directories</td></tr><tr><td><code>extra_skill_dirs</code></td><td><code>array&lt;string&gt;</code></td><td>—</td><td>Extra skill search directories, layered on top of the default directories</td></tr><tr><td><code>extra_agent_dirs</code></td><td><code>array&lt;string&gt;</code></td><td>—</td><td>Extra custom agent search directories, layered on top of the default directories</td></tr><tr><td><code>builtin_product_skills</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the built-in skills that document Kimi Code itself are offered to the model: <code>update-config</code>, <code>custom-theme</code>, <code>mcp-config</code>, <code>check-kimi-code-docs</code>, and <code>import-from-cc-codex</code>. Turning them off trims their names and descriptions from the system prompt, at the cost of the guided flows for those tasks. Read by the default <code>agent-core-v2</code> engine; ignored when <code>KIMI_CODE_LEGACY_FLAG=1</code> selects the legacy engine</td></tr><tr><td><code>telemetry</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether anonymous telemetry is enabled; disabled only when explicitly set to <code>false</code></td></tr><tr><td><code>providers</code></td><td><code>table</code></td><td><code>{}</code></td><td>API provider table → <a href="#providers"><code>providers</code></a></td></tr><tr><td><code>models</code></td><td><code>table</code></td><td>—</td><td>Model alias table → <a href="#models"><code>models</code></a></td></tr><tr><td><code>thinking</code></td><td><code>table</code></td><td>—</td><td>Default parameters for Thinking mode → <a href="#thinking"><code>thinking</code></a></td></tr><tr><td><code>loop_control</code></td><td><code>table</code></td><td>—</td><td>Agent loop control parameters → <a href="#loop-control"><code>loop_control</code></a></td></tr><tr><td><code>background</code></td><td><code>table</code></td><td>—</td><td>Background task runtime parameters → <a href="#background"><code>background</code></a></td></tr><tr><td><code>tools</code></td><td><code>table</code></td><td>—</td><td>Global tool switch → <a href="#tools"><code>tools</code></a></td></tr><tr><td><code>image</code></td><td><code>table</code></td><td>—</td><td>Image compression parameters → <a href="#image"><code>image</code></a></td></tr><tr><td><code>services</code></td><td><code>table</code></td><td>—</td><td>Built-in external service configuration → <a href="#services"><code>services</code></a></td></tr><tr><td><code>permission</code></td><td><code>table</code></td><td>—</td><td>Initial permission rules → <a href="#permission"><code>permission</code></a></td></tr><tr><td><code>hooks</code></td><td><code>array&lt;table&gt;</code></td><td>—</td><td>Lifecycle hooks; see <a href="./../customization/hooks.html">Hooks</a></td></tr><tr><td><code>identity</code></td><td><code>table</code></td><td>—</td><td>Custom agent identity → <a href="#identity"><code>identity</code></a></td></tr></tbody></table><p>The following sections cover each of the nested tables in turn: <code>providers</code>, <code>models</code>, <code>thinking</code>, <code>loop_control</code>, <code>background</code>, <code>tools</code>, <code>image</code>, <code>services</code>, and <code>permission</code>.</p><h2 id="providers" tabindex="-1"><code>providers</code> <a class="header-anchor" href="#providers" aria-label="Permalink to &quot;\`providers\`&quot;">​</a></h2><p>Each entry in the <code>providers</code> table defines an API provider, keyed by a unique name. The CLI reads credentials only from here — it does <strong>not</strong> fall back to shell environment variables automatically. Running <code>export KIMI_API_KEY</code> in the terminal does not give any provider its key; you must write it explicitly in the config file (see <a href="./overrides.html#provider-credentials">Config overrides</a>).</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td><code>string</code></td><td>Yes</td><td>Provider type: <code>kimi</code>, <code>anthropic</code>, <code>openai</code>, <code>openai_responses</code>, <code>google-genai</code>, <code>vertexai</code></td></tr><tr><td><code>api_key</code></td><td><code>string</code></td><td>No</td><td>API key, written in plain text in the config file</td></tr><tr><td><code>base_url</code></td><td><code>string</code></td><td>No</td><td>API base URL</td></tr><tr><td><code>oauth</code></td><td><code>table</code></td><td>No</td><td>OAuth credential reference (<code>storage</code> and <code>key</code> fields); injected automatically by the login flow — normally no need to write this by hand</td></tr><tr><td><code>env</code></td><td><code>table&lt;string, string&gt;</code></td><td>No</td><td>Fallback source for provider credentials; see below</td></tr><tr><td><code>custom_headers</code></td><td><code>table&lt;string, string&gt;</code></td><td>No</td><td>Custom HTTP headers attached to each request</td></tr></tbody></table><p><strong><code>env</code> sub-table</strong>: You can write provider-conventional key names (such as <code>KIMI_API_KEY</code>) inside <code>[providers.&lt;name&gt;.env]</code> as a fallback source for <code>api_key</code> / <code>base_url</code>. This sub-table is <strong>read only from the config file</strong> and does not modify the shell environment:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kimi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">env</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KIMI_API_KEY = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-xxx&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KIMI_BASE_URL = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.moonshot.ai/v1&quot;</span></span></code></pre></div><p>Priority: <code>api_key</code> field &gt; <code>env</code> sub-table key &gt; if both are absent, startup fails with an error.</p><h2 id="models" tabindex="-1"><code>models</code> <a class="header-anchor" href="#models" aria-label="Permalink to &quot;\`models\`&quot;">​</a></h2><p>Each entry in the <code>models</code> table defines a model alias (the name used in <code>default_model</code> or the <code>-m</code> flag), keyed by a unique name.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>provider</code></td><td><code>string</code></td><td>Yes</td><td>Name of the provider to use; must be defined in <code>providers</code></td></tr><tr><td><code>model</code></td><td><code>string</code></td><td>Yes</td><td>Model identifier sent to the server when calling the API</td></tr><tr><td><code>max_context_size</code></td><td><code>integer</code></td><td>Yes</td><td>Maximum context length in tokens; must be at least 1</td></tr><tr><td><code>max_input_size</code></td><td><code>integer</code></td><td>No</td><td>Declared per-request input limit when it sits below the total window (e.g. gpt-5: 400k window, 272k input). Compaction, context-overflow checks, and usage ratios prefer it; completion budgeting keeps the total window. Resolution clamps it to <code>max_context_size</code></td></tr><tr><td><code>max_output_size</code></td><td><code>integer</code></td><td>No</td><td>Per-request output token cap (maps to <code>max_tokens</code>). Currently only the <code>anthropic</code> provider honors it. When set for a Claude model, this explicit value overrides the built-in server-side maximum</td></tr><tr><td><code>capabilities</code></td><td><code>array&lt;string&gt;</code></td><td>No</td><td>Capability tags to add explicitly: <code>thinking</code>, <code>always_thinking</code>, <code>image_in</code>, <code>video_in</code>, <code>audio_in</code>, <code>tool_use</code>. Unioned with the capabilities auto-detected by the provider — entries can only be added, never removed</td></tr><tr><td><code>support_efforts</code></td><td><code>array&lt;string&gt;</code></td><td>No</td><td>Thinking effort levels the model accepts. For <code>kimi</code>, selecting another value at runtime fails; when model resolution carries an unsupported configured or previous value, the session falls back to the target model&#39;s <code>default_effort</code> and reports that effective value to the UI. A Thinking-capable Kimi model without this field uses boolean <code>on</code> / <code>off</code>. Other providers pass concrete values unchanged when their protocol has a native effort field; protocols that expose only levels or token budgets perform the required format conversion. Managed and open-platform refreshes may rewrite this field; to pin it manually, set <code>[models.&quot;&lt;alias&gt;&quot;.overrides] support_efforts</code> instead</td></tr><tr><td><code>default_effort</code></td><td><code>string</code></td><td>No</td><td>Default thinking effort for the model. Managed and open-platform refreshes may rewrite this field; to pin it manually, set <code>[models.&quot;&lt;alias&gt;&quot;.overrides] default_effort</code> instead</td></tr><tr><td><code>off_effort</code></td><td><code>string</code></td><td>No</td><td>Effort value sent on the wire to disable thinking (e.g. <code>none</code> for xai grok). Only meaningful for models that declare such an encoding (catalog imports set it): turning thinking Off then sends this value instead of omitting the effort field — the only way to actually stop reasoning on models that reason by default</td></tr><tr><td><code>base_url</code></td><td><code>string</code></td><td>No</td><td>Per-model endpoint override (written by catalog imports for gateway models served away from the provider default). Resolution prefers it over the provider&#39;s <code>base_url</code>; only takes effect together with <code>protocol</code></td></tr><tr><td><code>display_name</code></td><td><code>string</code></td><td>No</td><td>Name shown in the UI; falls back to <code>model</code> when unset</td></tr><tr><td><code>reasoning_key</code></td><td><code>string</code></td><td>No</td><td><code>openai</code> provider only. Override the field name used for reasoning content when the gateway returns it under a non-standard name; by default <code>reasoning_content</code>, <code>reasoning_details</code>, and <code>reasoning</code> are auto-detected</td></tr><tr><td><code>adaptive_thinking</code></td><td><code>boolean</code></td><td>No</td><td><code>anthropic</code> provider only. Force adaptive thinking on or off, overriding the version inference based on the model name. Omit to infer automatically (Claude ≥ 4.6 uses adaptive)</td></tr></tbody></table><p>When an alias contains <code>.</code>, use a quoted key:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;gpt-4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">provider = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;openai&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gpt-4.1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1047576</span></span></code></pre></div><h3 id="model-overrides" tabindex="-1">Model overrides <a class="header-anchor" href="#model-overrides" aria-label="Permalink to &quot;Model overrides&quot;">​</a></h3><p>Use <code>[models.&quot;&lt;alias&gt;&quot;.overrides]</code> for user overrides that must survive provider-model refreshes. Runtime consumers read the effective value: the override when present, otherwise the top-level field.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;kimi-code/kimi-for-coding&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">provider = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;managed:kimi-code&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-for-coding&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">262144</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">models</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">&quot;kimi-code/kimi-for-coding&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">overrides</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_context_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">131072</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">display_name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Kimi for Coding (custom)&quot;</span></span></code></pre></div><p><code>[models.&quot;&lt;alias&gt;&quot;.overrides]</code> accepts ordinary model fields such as <code>max_context_size</code>, <code>max_input_size</code>, <code>max_output_size</code>, <code>capabilities</code>, <code>display_name</code>, <code>reasoning_key</code>, <code>adaptive_thinking</code>, <code>support_efforts</code>, <code>default_effort</code>, and <code>off_effort</code>. It does not accept identity / routing fields: <code>provider</code>, <code>model</code>, <code>protocol</code>, <code>beta_api</code>, and <code>base_url</code>.</p><p>You can also switch models temporarily without touching the config file — by setting <code>KIMI_MODEL_*</code> environment variables, the CLI synthesizes a temporary provider in memory that does not persist after restart. See <a href="./env-vars.html#define-a-model-from-environment-variables-kimi-model">Define a model from environment variables</a>.</p><h2 id="secondary-model" tabindex="-1"><code>secondary_model</code> <a class="header-anchor" href="#secondary-model" aria-label="Permalink to &quot;\`secondary_model\`&quot;">​</a></h2><p>The secondary model is a second model configuration alongside the main model — typically a cheaper one, for features that do not need the main model&#39;s capability. Its consumer today is subagent spawning: when set, newly spawned subagents (<code>Agent</code> / <code>AgentSwarm</code>) bind to it by default instead of inheriting the main agent&#39;s model; when unset, subagents inherit the main agent&#39;s model.</p><p>This is a default binding, not a forced one. With the experiment enabled, the <code>Agent</code> / <code>AgentSwarm</code> tools gain a <code>model</code> parameter (accepting only the symbolic values <code>&quot;secondary&quot;</code> / <code>&quot;primary&quot;</code>), and the tool description lists the available models with the default marked. A spawn resolves the subagent&#39;s model in this order: an explicit tool-call <code>model</code> → the profile&#39;s <a href="./../customization/agents.html#agent-file-format"><code>model_preference</code></a> → the configured secondary model (the default). Here <code>&quot;primary&quot;</code> means the model the main agent is currently running, not necessarily <code>default_model</code> — for example after a mid-session <code>/model</code> switch.</p><p>Because overriding the default is the main agent&#39;s own decision (the tool description merely suggests <code>&quot;secondary&quot;</code> for routine tasks and <code>&quot;primary&quot;</code> for hard, quality-sensitive ones), there is no per-spawn switch on the user side. To steer a specific subagent to the main model, ask the main agent in your prompt to pass <code>model: &quot;primary&quot;</code>, or set <code>model_preference: &quot;primary&quot;</code> in the corresponding profile.</p><p>This feature is experimental and disabled by default. Enable it with <code>KIMI_CODE_EXPERIMENTAL_SECONDARY_MODEL=1</code>, or the master <code>KIMI_CODE_EXPERIMENTAL_FLAG=1</code>. It takes effect in every launch mode, including the interactive TUI.</p><p>In the interactive TUI, the <a href="./../reference/slash-commands.html"><code>/secondary_model</code></a> command opens a model picker that writes this section and live-applies it to the current session, so newly spawned subagents bind the new secondary model right away.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>model</code></td><td><code>string</code></td><td>—</td><td>The alias of a configured <a href="#models"><code>[models]</code></a> entry, e.g. <code>kimi-code/kimi-k2.5</code> (any provider, not limited to Kimi models)</td></tr><tr><td><code>default_effort</code></td><td><code>string</code></td><td>—</td><td>Thinking effort applied when subagents bind to the secondary model. Unset, the effort resolves naturally (global <code>[thinking]</code> config → the bound model&#39;s default effort) instead of inheriting the main agent&#39;s effort. Follows the main model&#39;s thinking-effort semantics: models with strict effort validation (e.g. Kimi models) fall back to their default effort for unsupported values; other providers receive the value as-is</td></tr><tr><td>Other fields</td><td>—</td><td>—</td><td>Accepts every field of <a href="#models"><code>[models.&quot;&lt;alias&gt;&quot;.overrides]</code></a> (<code>max_context_size</code>, <code>max_output_size</code>, <code>support_efforts</code>, …) as a model patch applied only to subagents</td></tr></tbody></table><p>Every field besides <code>model</code> forms a patch: when at least one patch field is set, the runtime synthesizes a derived model entry in memory (a copy of the pointed entry with the patch merged into its overrides, patch winning conflicts) and subagents bind that derived entry; with no patch fields, subagents bind the pointed entry directly. The derived entry lives only in memory (never written back to <code>config.toml</code>) and is hidden from model-selection lists.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">secondary_model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-code/kimi-k2.5&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">default_effort = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;low&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_output_size = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8192</span></span></code></pre></div><p><code>model</code> / <code>default_effort</code> can be overridden by the <code>KIMI_SECONDARY_MODEL</code> / <code>KIMI_SECONDARY_EFFORT</code> environment variables, which take higher priority than <code>config.toml</code>.</p><p>When the experiment is enabled, the configuration is validated as the session starts: an unresolvable <code>model</code>, or a <code>default_effort</code> not listed by the (patched) model, produces a startup warning (also returned by the session-warnings API). The check is advisory — a broken secondary model still fails at spawn time, with the same source hint attached to the spawn error.</p><h2 id="thinking" tabindex="-1"><code>thinking</code> <a class="header-anchor" href="#thinking" aria-label="Permalink to &quot;\`thinking\`&quot;">​</a></h2><p><code>thinking</code> sets the global default behavior for Thinking mode.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether Thinking is enabled by default for new sessions; set to <code>false</code> to force Thinking off</td></tr><tr><td><code>effort</code></td><td><code>string</code></td><td>—</td><td>Thinking effort level (for example <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, <code>max</code>). Non-Kimi providers do not remap concrete effort values when the upstream protocol accepts them; if the provider rejects the value, choose one that the model supports. Protocols that expose only levels or token budgets still require format conversion. Kimi models with <code>support_efforts</code> fall back to their model default when this configured value is not listed; Kimi models without that list treat every enabled value as boolean <code>on</code></td></tr><tr><td><code>keep</code></td><td><code>string</code></td><td><code>&quot;all&quot;</code></td><td>Preserved Thinking passthrough. On <code>kimi</code> it is sent as <code>thinking.keep</code>; on <code>anthropic</code> (Claude and Kimi&#39;s Anthropic-compatible mode) it is sent as a <code>context_management</code> <code>clear_thinking_20251015</code> edit (enabling keep routes Anthropic requests to the beta Messages API; an off-value disables keep and returns to the standard endpoint). <code>&quot;all&quot;</code> preserves prior turns&#39; reasoning (<code>reasoning_content</code> / Anthropic thinking blocks); set to an off-value (<code>false</code>/<code>0</code>/<code>no</code>/<code>off</code>/<code>none</code>/<code>null</code>) to disable. Overridden by <code>KIMI_MODEL_THINKING_KEEP</code>; only injected while Thinking is on</td></tr></tbody></table><h3 id="deprecated-fields" tabindex="-1">Deprecated fields <a class="header-anchor" href="#deprecated-fields" aria-label="Permalink to &quot;Deprecated fields&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Deprecated in</th><th>Description</th></tr></thead><tbody><tr><td><code>default_thinking</code></td><td>0.21.0</td><td>Top-level boolean, replaced by <code>[thinking] enabled</code>. Migrate <code>default_thinking = true</code> to <code>enabled = true</code>, and <code>default_thinking = false</code> to <code>enabled = false</code>.</td></tr><tr><td><code>thinking.mode</code></td><td>0.21.0</td><td>One of <code>auto</code> / <code>on</code> / <code>off</code>, replaced by <code>[thinking] enabled</code>. <code>mode = &quot;off&quot;</code> becomes <code>enabled = false</code>; <code>mode = &quot;on&quot;</code> and <code>mode = &quot;auto&quot;</code> are equivalent to <code>enabled = true</code> (the default) and can be removed.</td></tr><tr><td><code>loop_control.max_retries_per_step</code></td><td>0.32.0</td><td>Replaced by <code>loop_control.max_attempts_per_step</code> (the value was always a total-attempt limit, including the first try). The old key is ignored and reports a warning on startup; rename it in <code>config.toml</code>.</td></tr><tr><td><code>loop_control.max_steps_per_run</code></td><td>0.32.0</td><td>Replaced by <code>loop_control.max_steps_per_turn</code>. The old key is ignored and reports a warning on startup; rename it in <code>config.toml</code>.</td></tr></tbody></table><h2 id="loop-control" tabindex="-1"><code>loop_control</code> <a class="header-anchor" href="#loop-control" aria-label="Permalink to &quot;\`loop_control\`&quot;">​</a></h2><p><code>loop_control</code> governs the step count limit, the per-step attempt limit, and the threshold that triggers automatic context compaction in the Agent execution loop.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>max_steps_per_turn</code></td><td><code>integer</code></td><td>—</td><td>Maximum steps per turn; unset or <code>0</code> means unlimited</td></tr><tr><td><code>max_attempts_per_step</code></td><td><code>integer</code></td><td><code>10</code></td><td>Maximum total attempts for a failing step, including the initial attempt</td></tr><tr><td><code>reserved_context_size</code></td><td><code>integer</code></td><td>—</td><td>Number of tokens reserved for model output; automatic compaction is triggered when the remaining context window falls below this value</td></tr></tbody></table><p><code>max_steps_per_turn</code> can be overridden by the <code>KIMI_LOOP_MAX_STEPS_PER_TURN</code> environment variable, and <code>max_attempts_per_step</code> by <code>KIMI_LOOP_MAX_ATTEMPTS_PER_STEP</code>; both take higher priority than the config file. The former <code>KIMI_LOOP_MAX_RETRIES_PER_STEP</code> variable is deprecated but still honored (with a startup warning) when the new one is unset.</p><p>Retries only apply to transient failures — connection errors, timeouts, HTTP 429 rate limits, and 5xx server errors. A 429 caused by an exhausted quota or insufficient account balance is not retried and fails immediately, since it cannot succeed until the account is recharged.</p><h2 id="token-counting" tabindex="-1"><code>token_counting</code> <a class="header-anchor" href="#token-counting" aria-label="Permalink to &quot;\`token_counting\`&quot;">​</a></h2><p><code>token_counting</code> selects which context token count is reported externally — the value behind the context-size display. Internal logic (automatic compaction triggers, budgets, and overflow backoff) always uses both provider-reported usage and estimates, regardless of this setting.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>strategy</code></td><td><code>&quot;measured+estimated&quot; | &quot;measured&quot; | &quot;estimated&quot;</code></td><td><code>&quot;measured+estimated&quot;</code></td><td><code>measured+estimated</code> reports the live size — the provider-reported usage of each exchange plus an estimate of the not-yet-measured tail — floored by the last measured total; <code>measured</code> reports provider usage alone, so the display only moves when an exchange completes; <code>estimated</code> reports a pure estimate with provider usage ignored — the fallback for providers that do not report usage or report it unreliably</td></tr></tbody></table><p><code>strategy</code> can be overridden by the <code>KIMI_TOKEN_COUNTING_STRATEGY</code> environment variable, which takes higher priority than <code>config.toml</code>.</p><h2 id="background" tabindex="-1"><code>background</code> <a class="header-anchor" href="#background" aria-label="Permalink to &quot;\`background\`&quot;">​</a></h2><p><code>background</code> controls the concurrency behavior of background tasks (launched via the <code>Bash</code> tool or the <code>Agent</code> tool&#39;s <code>run_in_background=true</code> parameter).</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>max_running_tasks</code></td><td><code>integer</code></td><td>—</td><td>Maximum number of background tasks running concurrently</td></tr><tr><td><code>keep_alive_on_exit</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether to keep still-running background tasks when the session closes. By default, Kimi Code requests that all background tasks stop before the process exits; set this to <code>true</code> only when you want tasks to outlive the session. In print mode (<code>kimi -p</code>), this is only a legacy fallback used when <code>print_background_mode</code> is unset: <code>true</code> is equivalent to <code>print_background_mode = &quot;drain&quot;</code></td></tr><tr><td><code>kill_grace_period_ms</code></td><td><code>integer</code></td><td><code>5000</code></td><td>Grace period in milliseconds after session close, a manual stop, or a task timeout requests graceful termination. If a task is still running after this period, Kimi Code attempts to force-stop it</td></tr><tr><td><code>bash_auto_background_on_timeout</code></td><td><code>boolean</code></td><td><code>true</code></td><td>When a foreground <code>Bash</code> command hits its timeout, move it to a background task instead of killing it — the agent is notified when it completes, and the backgrounded command is bounded by the <code>bash_task_timeout_s</code> default background timeout. Set to <code>false</code> to kill timed-out foreground commands instead</td></tr><tr><td><code>bash_task_timeout_s</code></td><td><code>integer</code></td><td><code>600</code></td><td>Default timeout (seconds) for background <code>Bash</code> tasks when the call omits <code>timeout</code>; also used to re-arm foreground commands moved to the background on timeout. <code>0</code> means no timeout — the task runs until it exits or the model stops it. Explicit per-call <code>timeout</code> values are unaffected. In print mode (<code>kimi -p</code>) the default is <code>0</code> unless explicitly set</td></tr><tr><td><code>print_background_mode</code></td><td><code>&quot;exit&quot; | &quot;drain&quot; | &quot;steer&quot;</code></td><td><code>&quot;steer&quot;</code></td><td>Print mode (<code>kimi -p</code>) only. Governs how pending background tasks are handled once the main agent&#39;s turn ends: <code>&quot;exit&quot;</code> exits immediately; <code>&quot;drain&quot;</code> waits for every background task to reach a terminal state before exiting (results are not fed back to the main agent); <code>&quot;steer&quot;</code> stays alive so a completing background task — like a background subagent — injects a synthetic user message that steers the main agent into a new turn, looping until a turn ends with no pending background tasks or a limit is hit. Takes precedence over the <code>keep_alive_on_exit</code> print fallback</td></tr><tr><td><code>print_wait_ceiling_s</code></td><td><code>integer</code></td><td><code>2147483</code></td><td>In print mode (<code>kimi -p</code>), the wall-clock ceiling (seconds) for the wait/steer loop when <code>print_background_mode</code> is <code>&quot;drain&quot;</code> or <code>&quot;steer&quot;</code> (the default is ~24.8 days — effectively unbounded). Has no effect outside print mode or when it is <code>&quot;exit&quot;</code></td></tr><tr><td><code>print_max_turns</code></td><td><code>integer</code></td><td><code>100000</code></td><td>In print mode (<code>kimi -p</code>) with <code>print_background_mode = &quot;steer&quot;</code>, the maximum number of new turns that may be triggered by background-task completions, to keep the steering loop bounded (the default is effectively unbounded)</td></tr></tbody></table><p><code>keep_alive_on_exit</code> can be overridden by the <code>KIMI_CODE_BACKGROUND_KEEP_ALIVE_ON_EXIT</code> environment variable, and <code>max_running_tasks</code> by <code>KIMI_CODE_BACKGROUND_MAX_RUNNING_TASKS</code>; both take higher priority than <code>config.toml</code>.</p><p>In print mode (<code>kimi -p &quot;&lt;prompt&gt;&quot;</code>), Kimi Code stays alive after the main agent&#39;s turn as long as background tasks are still pending: each completion is fed back to the main agent as a synthetic user message, steering it into a new turn (<code>print_background_mode = &quot;steer&quot;</code> by default), and the run exits once a turn ends with nothing pending. The loop is bounded by <code>print_wait_ceiling_s</code> and <code>print_max_turns</code>, both effectively unbounded by default. Background work is never killed by a wall-clock cap in print mode either: background <code>Bash</code> tasks default to no timeout (<code>bash_task_timeout_s = 0</code>), and subagents run without a timeout (<code>[subagent] timeout_ms = 0</code>), so only the model itself stops a task. Set <code>print_background_mode</code> to <code>&quot;drain&quot;</code> to wait for tasks without feeding results back, or <code>&quot;exit&quot;</code> to end the run as soon as the main agent finishes.</p><h2 id="subagent" tabindex="-1"><code>subagent</code> <a class="header-anchor" href="#subagent" aria-label="Permalink to &quot;\`subagent\`&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>timeout_ms</code></td><td><code>integer</code></td><td><code>7200000</code> (2 hours)</td><td>Maximum wall-clock time (milliseconds) a single subagent (<code>Agent</code> / <code>AgentSwarm</code>) is allowed to run before it is settled as <code>timed_out</code>. <code>0</code> means no timeout — the subagent runs until it finishes or the model stops it. This is the background-task manager&#39;s per-task timeout for each subagent task, so it applies to both foreground and background subagents. In print mode (<code>kimi -p</code>) the default is <code>0</code> unless explicitly set. Note: any value above <code>2147483647</code> (about 24.8 days) is clamped to roughly 24.8 days by the runtime</td></tr><tr><td><code>team_mode</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Team mode master switch. When off, the five management tools (<code>TeamHire</code>, <code>TeamFire</code>, <code>TeamScore</code>, <code>TeamMessage</code>, <code>TeamConcurrency</code>) are hidden from the main Agent. <code>/team on|off</code> toggles it from the terminal (writes to this field, takes effect on the next request). Only gates tool visibility — model bindings, role, duty, etc. are unaffected</td></tr><tr><td><code>max_concurrency</code></td><td><code>integer</code></td><td>Unlimited</td><td>Session-level subagent concurrency cap. Priority: <code>KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY</code> env var &gt; runtime <code>TeamConcurrency</code> setting &gt; this field. Must be ≥ 1 when set</td></tr><tr><td><code>model_overrides</code></td><td><code>table</code></td><td>—</td><td>profile name → model id mapping, used to override <code>model_preference</code> per-member during conversation (lower priority than explicit tool parameter, higher than frontmatter <code>model_preference</code>). Example: see below</td></tr></tbody></table><p><code>timeout_ms</code> can be overridden by the <code>KIMI_SUBAGENT_TIMEOUT_MS</code> environment variable, which takes higher priority than <code>config.toml</code>.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># chat-level override: coder stops following secondary and uses kimi-for-coding instead</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subagent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model_overrides</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coder = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-code/kimi-for-coding&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">reviewer = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;kimi-code/k3&quot;</span></span></code></pre></div><h2 id="mcp" tabindex="-1"><code>mcp</code> <a class="header-anchor" href="#mcp" aria-label="Permalink to &quot;\`mcp\`&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>startup_timeout_ms</code></td><td><code>integer</code></td><td><code>30000</code> (30 seconds)</td><td>Global default connection (startup + tool discovery) timeout in milliseconds for all MCP servers. Accepts <code>1</code>–<code>2147483647</code>. A per-server <code>startupTimeoutMs</code> in <code>mcp.json</code> always wins over this section and the environment variable; when neither is set, the default applies</td></tr><tr><td><code>tool_timeout_ms</code></td><td><code>integer</code></td><td><code>60000</code> (60 seconds)</td><td>Global default single tool-call timeout in milliseconds for all MCP servers. Accepts <code>1</code>–<code>2147483647</code>. A per-server <code>toolTimeoutMs</code> in <code>mcp.json</code> always wins over this section and the environment variable; when neither is set, the client built-in default applies</td></tr></tbody></table><p><code>startup_timeout_ms</code> and <code>tool_timeout_ms</code> can be overridden by the <code>KIMI_MCP_STARTUP_TIMEOUT_MS</code> and <code>KIMI_MCP_TOOL_TIMEOUT_MS</code> environment variables respectively, which take higher priority than <code>config.toml</code>. See <a href="./../customization/mcp.html">MCP</a> for the full MCP server configuration.</p><h2 id="identity" tabindex="-1"><code>identity</code> <a class="header-anchor" href="#identity" aria-label="Permalink to &quot;\`identity\`&quot;">​</a></h2><p>Customizes how the agent identifies itself. Leave it unset and nothing changes.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>string</code></td><td>—</td><td>Display name the agent calls itself in the system prompt (fills the <code>\${product_name}</code> slot, including in your own <code>SYSTEM.md</code> and agent files)</td></tr><tr><td><code>slug</code></td><td><code>string</code></td><td>derived from <code>name</code></td><td>Machine identifier used in protocol fields: the <code>User-Agent</code> product token sent to third-party providers, and the client name announced to MCP servers. Derived from <code>name</code> when omitted: lowercased, with every run of non-alphanumeric characters folded to <code>-</code></td></tr></tbody></table><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">identity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">name = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Acme Dev Agent&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">slug = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acme-dev&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # optional</span></span></code></pre></div><p>Both fields can be set through the <code>KIMI_CODE_IDENTITY_NAME</code> and <code>KIMI_CODE_IDENTITY_SLUG</code> environment variables, which take higher priority than <code>config.toml</code> and are never written back to it — convenient for containers and CI, where writing a config file is awkward.</p><p>A name that contains no ASCII letters or digits (for example a purely Chinese name) leaves nothing to derive a slug from and falls back to <code>agent</code>; write <code>slug</code> explicitly if you need a specific protocol token.</p><p>The identity is resolved once at startup and holds for the life of the process — it is announced to MCP servers and providers when connections are made, so it cannot change midway. Edits to this section take effect on the next start, for new sessions: a resumed session keeps the system prompt it was recorded with, since its past turns already speak under that identity. Likewise, an MCP OAuth authorization keeps the client registration it was granted under; reset that server&#39;s authentication to register under the new identity.</p><p>This section is read by the default <code>agent-core-v2</code> engine. It is ignored by the legacy <code>kimi</code> / <code>kimi -p</code> path selected with <code>KIMI_CODE_LEGACY_FLAG=1</code>; <code>kimi web</code> always uses <code>agent-core-v2</code>.</p><h2 id="tools" tabindex="-1"><code>tools</code> <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;\`tools\`&quot;">​</a></h2><p><code>tools</code> is the global tool switch: it applies to every agent in all sessions and intersects with each agent&#39;s own <code>tools</code> / <code>disallowedTools</code> policy.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>array&lt;string&gt;</code></td><td>—</td><td>Global allowlist: when non-empty, only the listed tools are available; omitting the field or setting an empty array imposes no constraint</td></tr><tr><td><code>disabled</code></td><td><code>array&lt;string&gt;</code></td><td>—</td><td>Global denylist, applied after <code>enabled</code></td></tr></tbody></table><p>Name matching follows the same rules as the same-named fields in an agent file: built-in tools match by exact name (such as <code>Read</code>), and MCP tools match with globs (such as <code>mcp__github__*</code>). Three entry shapes never match anything and are reported with a warning: a wildcard outside an <code>mcp__</code> pattern (<code>enabled = [&quot;*&quot;]</code> disables every tool, <code>disabled = [&quot;*&quot;]</code> disables none), an <code>mcp__</code> literal missing the tool segment (<code>mcp__github</code> — use <code>mcp__github__*</code> for a whole server), and a name no registered or built-in tool has (matching is case-sensitive).</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tools</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disabled = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;EnterPlanMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ExitPlanMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mcp__github__*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Note</p><p>Like the <code>tools</code> / <code>disallowedTools</code> fields of an agent file, this section shapes the tools shown to the model and is enforced again before execution. <a href="#permission">Permission rules</a> remain a separate control for operations that require approval.</p></div><h2 id="image" tabindex="-1"><code>image</code> <a class="header-anchor" href="#image" aria-label="Permalink to &quot;\`image\`&quot;">​</a></h2><p><code>image</code> controls how images are compressed before being sent to the model, across every ingestion point (pasted images, <code>ReadMediaFile</code> reads, images in MCP tool results, and so on).</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>max_edge_px</code></td><td><code>integer</code></td><td><code>2000</code></td><td>Longest-edge ceiling in pixels. Larger images are scaled down proportionally to fit; raising it preserves more detail at the cost of larger request bodies</td></tr><tr><td><code>read_byte_budget</code></td><td><code>integer</code></td><td><code>262144</code> (256 KB)</td><td>Per-image byte budget for images the model reads for itself (<code>ReadMediaFile</code> default reads). It bounds the accumulated request-body size when the model keeps screenshotting and reading images; fine detail stays reachable through the <code>region</code> parameter, which reads a crop back at full fidelity (<code>region</code> and <code>full_resolution</code> are not subject to this budget)</td></tr></tbody></table><p><code>max_edge_px</code> can be overridden by the <code>KIMI_IMAGE_MAX_EDGE_PX</code> environment variable and <code>read_byte_budget</code> by <code>KIMI_IMAGE_READ_BYTE_BUDGET</code>; both take higher priority than <code>config.toml</code>.</p><h2 id="services" tabindex="-1"><code>services</code> <a class="header-anchor" href="#services" aria-label="Permalink to &quot;\`services\`&quot;">​</a></h2><p><code>services</code> configures two built-in services: web search (<code>moonshot_search</code>) and web fetch (<code>moonshot_fetch</code>). Only these two fixed keys are recognized; other keys are ignored. Both entries share the same fields:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>base_url</code></td><td><code>string</code></td><td>No</td><td>Service API URL</td></tr><tr><td><code>api_key</code></td><td><code>string</code></td><td>No</td><td>API key</td></tr><tr><td><code>oauth</code></td><td><code>table</code></td><td>No</td><td>OAuth credential reference, same structure as <code>providers.*.oauth</code></td></tr><tr><td><code>custom_headers</code></td><td><code>table&lt;string, string&gt;</code></td><td>No</td><td>Custom HTTP headers attached to each request</td></tr></tbody></table><p><code>base_url</code> and <code>api_key</code> can also come from environment variables, which take priority over the config file: <code>KIMI_WEB_SEARCH_BASE_URL</code> / <code>KIMI_WEB_SEARCH_API_KEY</code> for <code>moonshot_search</code>, and <code>KIMI_WEB_FETCH_BASE_URL</code> / <code>KIMI_WEB_FETCH_API_KEY</code> for <code>moonshot_fetch</code>. An env base URL defines a separate service endpoint, so the persisted API key, OAuth reference, and custom headers are not forwarded to it; set the matching env API key when that endpoint requires authentication. An env API key without an env base URL keeps the configured endpoint and custom headers but replaces both configured credential forms. Setting the base URL and API key through env without any config section also enables the service.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">moonshot_search</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.moonshot.cn/v1/search&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-xxx&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">moonshot_fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base_url = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://api.moonshot.cn/v1/fetch&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api_key = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sk-xxx&quot;</span></span></code></pre></div><h2 id="permission" tabindex="-1"><code>permission</code> <a class="header-anchor" href="#permission" aria-label="Permalink to &quot;\`permission\`&quot;">​</a></h2><p><code>permission</code> sets permission rules that are automatically loaded when a session starts, controlling whether the Agent needs user confirmation before calling a tool. Rules are written as a <code>[[permission.rules]]</code> array of tables, matched in order — the first matching rule takes effect.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>decision</code></td><td><code>string</code></td><td>Yes</td><td>Action on match: <code>allow</code> (permit immediately), <code>deny</code> (reject immediately), <code>ask</code> (prompt each time)</td></tr><tr><td><code>scope</code></td><td><code>string</code></td><td>No</td><td>Rule scope: <code>turn-override</code>, <code>session-runtime</code>, <code>project</code>, <code>user</code>; defaults to <code>user</code></td></tr><tr><td><code>pattern</code></td><td><code>string</code></td><td>Yes</td><td>Match pattern in the form <code>ToolName</code> or <code>ToolName(arg-pattern)</code>, e.g. <code>Read</code> or <code>Bash(rm -rf*)</code></td></tr><tr><td><code>reason</code></td><td><code>string</code></td><td>No</td><td>Rule description for debugging and auditing</td></tr></tbody></table><p>Built-in tool names are listed in <a href="./../reference/tools.html">Built-in tools</a>. Most built-in tools that accept rule arguments define their own matching subject, such as <code>Bash(command-pattern)</code> or <code>Read(path-pattern)</code>. <code>AgentSwarm</code>, MCP tools, and custom tools can only be matched by tool name — argument patterns are not supported for them.</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allow&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Read&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;allow&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Grep&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;deny&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash(rm -rf*)&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">permission</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">decision = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ask&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pattern = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>MCP server declarations are configured in <code>~/.kimi-code/mcp.json</code> or the project-local <code>.kimi-code/mcp.json</code>, not in <code>config.toml</code>. The interactive configuration entry point is <code>/mcp-config</code>; see <a href="./../customization/mcp.html">Model Context Protocol</a>.</p></div><h2 id="tui-toml" tabindex="-1"><code>tui.toml</code> <a class="header-anchor" href="#tui-toml" aria-label="Permalink to &quot;\`tui.toml\`&quot;">​</a></h2><p>Alongside <code>config.toml</code>, the CLI keeps terminal-UI and client preferences in a companion <code>tui.toml</code> in the same directory (<code>~/.kimi-code/tui.toml</code>, or <code>$KIMI_CODE_HOME/tui.toml</code> when overridden). It is created with defaults on first run, and the interactive commands <code>/config</code>, <code>/theme</code>, and <code>/editor</code> write to it for you — so you rarely need to edit it by hand. If the file is malformed, the CLI falls back to defaults and shows a notice instead of failing to start.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>theme</code></td><td><code>string</code></td><td><code>auto</code></td><td>Color theme: <code>auto</code> (follow the terminal), <code>dark</code>, <code>light</code>, or the name of a <a href="./../customization/themes.html">custom theme</a></td></tr><tr><td><code>disable_paste_burst</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Disable the non-bracketed paste-burst fallback that keeps rapid multi-line pastes from submitting line by line</td></tr><tr><td><code>cache_expiry_hint</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Show a dialog when resuming a long-idle session or submitting after a long idle stretch, warning that the context cache has likely expired and offering to compact or start a new session (v2 engine only)</td></tr><tr><td><code>[editor].command</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td><td>External editor command for composing long input; empty falls back to <code>$VISUAL</code> / <code>$EDITOR</code></td></tr><tr><td><code>[notifications].enabled</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether desktop notifications are sent</td></tr><tr><td><code>[notifications].notification_condition</code></td><td><code>string</code></td><td><code>unfocused</code></td><td>When to notify: <code>unfocused</code> (only when the terminal is not focused) or <code>always</code></td></tr><tr><td><code>[upgrade].auto_install</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether new versions are installed automatically</td></tr><tr><td><code>[status_line].items</code></td><td><code>string[]</code></td><td><code>[]</code></td><td>Built-in slots to show on the first footer line and their order: <code>mode</code>, <code>goal</code>, <code>model</code>, <code>tasks</code>, <code>cwd</code>, <code>git</code>, <code>tips</code>. Unset keeps the default layout; unknown ids are skipped with a warning</td></tr><tr><td><code>[status_line].command</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td><td>Custom status line command. Its first stdout line replaces the first footer line, with a JSON snapshot (model, cwd, git branch, permission mode, plan mode, context usage, session id, version) passed on stdin. Runs are capped at 300ms and throttled to once per second; failures fall back to the built-in layout</td></tr></tbody></table><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ~/.kimi-code/tui.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">theme = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auto&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # &quot;auto&quot; | &quot;dark&quot; | &quot;light&quot; | custom theme name</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">disable_paste_burst = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # true disables non-bracketed paste-burst fallback</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cache_expiry_hint = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # false disables the &quot;cache expired&quot; dialog on resume / idle submit</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">editor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">command = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # empty uses $VISUAL / $EDITOR</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">notifications</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">enabled = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">notification_condition = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;unfocused&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # &quot;unfocused&quot; | &quot;always&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">upgrade</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">auto_install = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># [status_line]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># items = [&quot;mode&quot;, &quot;goal&quot;, &quot;model&quot;, &quot;tasks&quot;, &quot;cwd&quot;, &quot;git&quot;, &quot;tips&quot;]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># command = &quot;~/.kimi-code/statusline.sh&quot;</span></span></code></pre></div><p>Changes apply on the next start, or immediately with <code>/reload-tui</code> (which reloads only <code>tui.toml</code>); <code>/reload</code> reloads both <code>config.toml</code> and <code>tui.toml</code>.</p><h2 id="project-local-configuration" tabindex="-1">Project-local configuration <a class="header-anchor" href="#project-local-configuration" aria-label="Permalink to &quot;Project-local configuration&quot;">​</a></h2><p>In addition to the user-level files under <code>~/.kimi-code</code>, Kimi Code reads a project-local configuration file at <code>&lt;project-root&gt;/.kimi-code/local.toml</code>. It holds settings that are specific to one project checkout and typically should not be shared with teammates.</p><p>The file is created automatically when you add an extra workspace directory with <a href="./../reference/slash-commands.html"><code>/add-dir</code></a> and choose to remember it for the project. You rarely need to edit it by hand.</p><h3 id="workspace" tabindex="-1"><code>[workspace]</code> <a class="header-anchor" href="#workspace" aria-label="Permalink to &quot;\`[workspace]\`&quot;">​</a></h3><p>The <code>[workspace]</code> table groups project-level workspace settings:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>additional_dir</code></td><td><code>array&lt;string&gt;</code></td><td>No</td><td>Additional workspace directories, stored as absolute paths. Written automatically when you confirm &quot;remember this directory&quot; in <code>/add-dir</code>; read back on startup so the directories are available in every session of this project</td></tr></tbody></table><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">workspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">additional_dir = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/absolute/path/to/shared&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span></code></pre></div><p>Because directories are stored as absolute paths, which are specific to your machine, we recommend adding <code>.kimi-code/local.toml</code> to your project&#39;s <code>.gitignore</code> so it is not committed.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="./providers.html">Providers and models</a> — connection examples for each provider type (Kimi, Claude, OpenAI, Gemini)</li><li><a href="./overrides.html">Config overrides</a> — priority rules for CLI options, config file, and environment variables</li><li><a href="./env-vars.html">Environment variables</a> — complete list of runtime variables like <code>KIMI_CODE_HOME</code></li></ul>`,113)])])}const u=t(a,[["render",d]]);export{k as __pageData,u as default};
